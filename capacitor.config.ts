import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'starter-app-modals',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
